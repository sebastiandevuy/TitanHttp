    import XCTest
    @testable import TitanHttp

    final class TitanHttpTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(TitanHttp().text, "Hello, World!")
        }
    }
